/target

# Swift build detritus from an empty scaffold: no sources, only .build.
# The real app is in wisent-ai/skarbiec-desktop.
/desktop/
skarbiec.vault.json
skarbiec.audit.jsonl
*.vault.json
*.audit.jsonl
# The journal's flock sibling. Ignoring the journal but not its lock left a
# runtime artifact deciding whether the tree looked publishable.
*.audit.jsonl.lock
.vault-resolved/
vault.enc.json
*.local.json
.env
.env.*
!.env.example

# Git worktrees checked out inside the repository; never repository content.
.worktrees/
