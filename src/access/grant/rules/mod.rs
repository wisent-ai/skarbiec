// The rules a grant is written against: the names it may use, and the
// capabilities it may declare with them.

pub(in crate::access::grant) mod capabilities;
pub(in crate::access::grant) mod validation;
